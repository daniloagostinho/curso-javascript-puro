// Estrutura de controle de fluxo (if e else)

let age = 30;
let temperatura = 30;

if(temperatura < 0) {
    // codigo para ser executado se a condicao for verdadeira
    console.log('está muito frio')
} else if (temperatura <= 20) {
    // codigo para ser executado se a condicao for false
    console.log('está frio')
} else {
    console.log('Está quente!')
}
